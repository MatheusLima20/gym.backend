export class RegisterItemController {

    async handle(request: Request, response: Response) {

        /*const { id, name, price } =
            request.body;

        const repository =
            new PrismaItemRepository();

        const useCase =
            new RegisterItemUseCase(
                repository
            );

        const result =
            await useCase.execute({
                id,
                name,
                price
            });

        return response.json(result);*/
    }
}